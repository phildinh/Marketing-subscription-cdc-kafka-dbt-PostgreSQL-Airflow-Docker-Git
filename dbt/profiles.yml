cdc_subscription:
  target: dev
  outputs:
    dev:
      type: postgres
      host: "{{ env_var('ANALYTICAL_DB_HOST', 'localhost') }}"
      port: 5432
      dbname: analytical_db
      user: postgres
      password: "{{ env_var('ANALYTICAL_DB_PASSWORD') }}"
      schema: raw
      threads: 4
